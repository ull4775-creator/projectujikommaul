@extends('layouts.app')

@section('title', 'Daftar Laporan Pengaduan')

@section('content')
<div class="container-fluid">
    <h2 class="mb-4">📋 Daftar Laporan Pengaduan</h2>

    @if(session('success'))
        <div class="alert alert-success alert-dismissible fade show">
            {{ session('success') }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    @endif

    <div class="card shadow-sm">
        <div class="card-body table-responsive">
            <table class="table table-striped table-hover align-middle" id="tabelinput">
                <thead class="table-dark text-center">
                    <tr>
                        <th>No</th>
                        <th>Foto</th>
                        <th>No Sambungan</th>
                        <th>Alamat</th>
                        <th>Kategori</th>
                        <th>Lokasi</th>
                        <th>Keterangan</th>
                        <th>Kode</th>
                        <th>Status</th>
                        <th>Aksi</th>
                    </tr>
                </thead>
                <tbody>
    @forelse($pengaduans as $input)
    <tr>
        <td class="text-center">{{ $loop->iteration }}</td>
        <td class="text-center">
            @if($input->foto)
                <img src="{{ asset('uploads/'.$input->foto) }}" style="max-width:80px;" class="rounded shadow-sm">
            @else
                <span class="text-muted small"><i>Tidak ada foto</i></span>
            @endif
        </td>
        <td>{{ $input->no_sa }}</td>
        <td>{{ Str::limit($input->alamat_lengkap, 30) }}</td> {{-- Gunakan nama kolom yg benar --}}
        <td>{{ $input->kategori->nama_kategori ?? 'Umum' }}</td>
        <td>{{ $input->lokasi_kejadian ?? '-' }}</td> {{-- Gunakan nama kolom yg benar --}}
        <td>{{ Str::limit($input->ket, 30) }}</td>
        <td><code class="text-primary">{{ $input->kode ?? '-' }}</code></td>
        <td class="text-center">
            <span class="badge 
                @if($input->status == 'menunggu') bg-primary
                @elseif($input->status == 'proses') bg-warning text-dark
                @elseif($input->status == 'selesai') bg-success
                @else bg-secondary @endif">
                {{ ucfirst($input->status) }}
            </span>
        </td>
        <td class="text-center">
            <div class="btn-group">
                <a href="{{ route('admin.pengaduan.detail', $input->id) }}" class="btn btn-sm btn-info text-white">
                    <i class="fas fa-eye"></i>
                </a>
                <form action="{{ route('admin.pengaduan.destroy', $input->id) }}" method="POST" class="d-inline">
                    @csrf
                    @method('DELETE')
                    <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Yakin ingin menghapus laporan ini?')">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </div>
        </td>
    </tr>
    @empty
    <tr>
        <td colspan="10" class="text-center text-muted py-4">Belum ada data laporan yang masuk.</td>
    </tr>
    @endforelse
</tbody>
            </table>
        </div>
    </div>
</div>

@push('scripts')
<script>
    $(document).ready(function () {
        $('#tabelinput').DataTable({
            "responsive": true,
            "lengthChange": false,
            "autoWidth": false,
        });
    });
</script>
@endpush
@endsection
